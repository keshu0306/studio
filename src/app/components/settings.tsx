"use client";

export const Settings = () => {
  return (
    <div>
      <h2>Settings</h2>
      <p>Configure USB port access and malware detection settings here.</p>
      {/* Add settings configuration options here */}
    </div>
  );
};
